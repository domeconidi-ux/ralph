## 2026-02-25 - US-051
- Created app/(protected)/scan-receipt-review.tsx — OCR review + edit + save screen
- Added `PendingReceipt` interface and `pendingReceipt`/`setPendingReceipt` to store/coupleStore.ts (same pattern as pendingInviteCode)
- Updated app/(protected)/scan-receipt.tsx: capturePhoto now uses `base64: true` in takePictureAsync; openGallery uses `base64: true` in launchImageLibraryAsync; both store result in coupleStore.pendingReceipt + navigate to /scan-receipt-review instead of router.back()
- Review screen flow: (1) reads pendingReceipt from store; (2) on mount, calls useReceiptScanner.scan(base64) via useRef guard to avoid double-call; (3) shows loading state with Image thumbnail while isScanning; (4) pre-fills form via setValue() when result arrives
- Form: identical to add.tsx — RHF + zodResolver(expenseSchema), all fields editable (amount, description, CategoryPicker, date, SplitSelector)
- Save logic: exact copy of add.tsx insert flow (insert expense → compute splits → insert splits → rollback on error) + invalidate() + router.replace("/(protected)/(tabs)/expenses") + setPendingReceipt(null)
- "Riscan" button: setPendingReceipt(null) + router.back() → returns user to scan-receipt camera screen
- Error handling: scanError shows amber warning banner ("verifica i dati"), keeps form usable for manual correction; apiError shows red banner on save failure
- No new packages needed — Image from react-native displays local URIs directly; base64 captured by expo-camera/expo-image-picker options
- TypeScript: SUCCESS (exit 0), Jest: 54/54 PASS
- Committed: 1231699
- **Learnings for future iterations:**
  - `pendingReceipt` in coupleStore (uri + base64) is the pattern for ephemeral image state between scan and review screens — same as pendingInviteCode for cross-screen ephemeral state
  - `takePictureAsync({ quality: 0.8, base64: true })` returns `{ uri, base64, width, height }` — base64 may be undefined if device fails, always guard with `if (photo?.base64)`
  - `launchImageLibraryAsync({ base64: true })` adds base64 to `assets[0].base64` — same guard needed
  - `useRef(false)` guard for one-shot OCR scan on mount: `if (!pendingReceipt || hasScanned.current) return; hasScanned.current = true; scan(...)` — prevents double-call in React StrictMode or re-renders
  - `setValue("category", result.category.toLowerCase())` normalizes OCR category to match CATEGORIES ids (all lowercase)
  - `useExpenses().invalidate()` should be called before router.replace when saving from review screen — ensures expenses list is refreshed before navigation
---

## 2026-02-25 - US-050
- Created hooks/useReceiptScanner.ts — OCR receipt hook using OpenAI Vision API
- Uses `fetch` (no OpenAI SDK) to call `https://api.openai.com/v1/chat/completions` with model `gpt-4o-mini`
- Vision request: multimodal content array with `image_url` (data:image/jpeg;base64,${base64}) + `text` prompt in Italian
- Structured Outputs: `response_format.type = "json_schema"` with strict schema for `{ amount, description, category, date, items[] }` — all fields required, `additionalProperties: false` at all levels
- PII purification: `purifyText(content)` applied on the raw JSON string from OpenAI before `JSON.parse()` — safe since Italian PII patterns don't overlap JSON structural characters
- OCR usage registration: `supabase.from("ocr_usage").insert({ user_id, couple_id })` after successful parse
- API key: `process.env.EXPO_PUBLIC_OPENAI_API_KEY ?? ""` — added placeholder entry to `.env.local` (gitignored)
- Exposes: `scan(imageBase64: string)`, `isScanning`, `error`, `result`
- Exported interfaces: `ReceiptItem` and `ReceiptData` for consumers
- TypeScript: SUCCESS (exit 0), Jest: 54/54 PASS
- Committed: da5a873
- **Learnings for future iterations:**
  - `fetch` to OpenAI API requires `Authorization: Bearer ${key}` header — no SDK needed for simple Vision calls
  - OpenAI Structured Outputs `strict: true` requires: (1) all properties listed in `required`, (2) `additionalProperties: false` on every `object` (including nested), (3) no optional properties at schema level
  - PII purification on the raw JSON string (before `JSON.parse`) is safe: Italian PII regex patterns (codice fiscale, IBAN, credit card, phone) are word-bounded and won't match JSON syntax characters
  - `useCoupleStore((state) => state.couple)` selector pattern (vs destructuring) used to avoid subscribing to the entire store; consistent with other hooks that read couple_id
  - `.env.local` is gitignored — only document the key name (placeholder entry with empty value); never expose real API keys
---

## 2026-02-25 - US-049
- Created app/(protected)/scan-receipt.tsx — full receipt scanner screen
- Checks feature gating on mount: canUseReceiptScanner(user.id) → isLimitReached state; shows upgrade message with "Scopri Premium" CTA if limit hit
- Checks camera permissions via useCameraPermissions hook; shows "Consenti accesso" (Pressable → requestPermissions) if canAskAgain, else "Apri impostazioni" (Linking.openSettings())
- Main camera view: CameraView from expo-camera with `facing="back"` and ref for takePictureAsync
- Rectangular guide overlay: 4-panel semi-transparent layout (top/bottom flex:1 + left/right flex:1 flanking a 260×380 bordered rectangle); hint text in bottom panel
- Header hidden via `<Stack.Screen options={{ headerShown: false }} />` for full-screen camera immersion
- GDPR consent: Alert.alert dialog shown on first capture/gallery press; gdprConsented state prevents re-showing dialog in same session
- Capture button: triggers cameraRef.current.takePictureAsync({ quality: 0.8 }); routes back on success
- Gallery button: triggers ImagePicker.launchImageLibraryAsync({ mediaTypes: "images" as MediaType, quality: 0.8 }); routes back on non-canceled result
- Close button: router.back() from camera view
- Loading state: ActivityIndicator shown while permLoading || isCheckingLimit
- TypeScript: SUCCESS (exit 0), Jest: 54/54 PASS
- Committed: 76d0586
- **Learnings for future iterations:**
  - `Stack.Screen options={{ headerShown: false }}` inside the screen component (not layout) hides the expo-router header for that specific screen — correct pattern for full-screen camera/modal screens
  - CameraView ref typed as `useRef<CameraView>(null)` — works correctly with TypeScript since CameraView is a React class component type
  - ImagePicker v17 `mediaTypes` field accepts `"images" as ImagePicker.MediaType` (string literal cast) — the old `MediaTypeOptions.Images` enum is deprecated in v17
  - Semi-transparent overlay "cutout" in React Native: 4-panel approach (top/left/right/bottom Views with backgroundColor rgba) is the reliable pattern — no CSS clip-path available in RN
  - `canUseReceiptScanner` returns false on DB error (deny access) — calling it on mount with user.id is the correct pattern; wrap in useEffect([user])
---

## 2026-02-25 - US-048
- Created supabase/migrations/006_ocr_usage.sql: table ocr_usage (id, user_id FK auth.users RESTRICT, couple_id nullable FK couples SET NULL, created_at); indexes on user_id, couple_id, created_at; RLS enabled with select_own and insert_own policies
- Created lib/featureGating.ts: exports canUseReceiptScanner(userId: string): Promise<boolean>
- canUseReceiptScanner: computes startOfMonth = new Date(year, month, 1).toISOString(), queries ocr_usage with .select("id", { count: "exact", head: true }).eq("user_id").gte("created_at", startOfMonth), returns count < LIMITS.free.ocrScansPerMonth (30)
- On DB error: returns false (deny access for safety)
- LIMITS.free.ocrScansPerMonth = 30 sourced from @/constants (pricing.ts) — no magic number
- TypeScript: SUCCESS (exit 0), Jest: 54/54 PASS (no new tests — featureGating imports Supabase client which requires RN runtime, not testable in Node/Jest)
- **Learnings for future iterations:**
  - featureGating.ts imports supabase client (which uses expo-secure-store); it is NOT a pure lib/ function and cannot be unit-tested in Jest without mocking — this is intentional; the module is a thin adapter between the DB and the app
  - Supabase count query pattern: `.select("id", { count: "exact", head: true })` — `head: true` avoids fetching actual rows (only the count header); returns `{ count: number | null, error }` (not `data`)
  - `new Date(year, month, 1)` gives the first day of the current month (month is 0-indexed in JS, same month value works)
  - couple_id FK on ocr_usage uses ON DELETE SET NULL (not CASCADE) — usage records are preserved even if a couple is dissolved; user_id uses RESTRICT to preserve records when user is deleted
---

## 2026-02-25 - US-047
- Created lib/piiPurifier.ts with purifyText(text: string): string
- Four regex patterns: CODICE_FISCALE_REGEX, CREDIT_CARD_REGEX, IBAN_REGEX, PHONE_IT_REGEX
- Codice fiscale: `/\b[A-Z]{6}\d{2}[A-Z]\d{2}[A-Z]\d{3}[A-Z]\b/gi` (16-char Italian fiscal code format)
- Credit card: `/\b\d{4}[\s\-]?\d{4}[\s\-]?\d{4}[\s\-]?\d{4}\b/g` (16 digits, optional grouping separators)
- IBAN: alternation covering compact (`IT` + 25 alphanumeric) and space-grouped (`IT60 X054 2811 ...`) formats
- Phone IT: covers mobile (3xx) and landline (0xx) with optional +39 country code and space/dash separators
- All PII replaced with `[REDACTED]` constant
- Created __tests__/lib/piiPurifier.test.ts with 15 test cases across 5 describe blocks
- Tests cover: codice fiscale (3 cases), credit card (3), IBAN (3), phone (3), mixed text (3)
- TypeScript: SUCCESS (exit 0), Jest: 54/54 PASS (15 new + 39 existing)
- Committed: c40c2bb
- **Learnings for future iterations:**
  - Apply replacements in order: codice fiscale → IBAN → credit card → phone; IBAN must precede credit card to avoid partial matches on the digit segments of IBANs
  - Italian IBAN has two display formats: compact (IT60X0542811101000000123456) and space-grouped (IT60 X054 2811 ...); use alternation to cover both
  - `purifyText` is a pure function (no React Native or @/ deps) — directly importable in Jest without module mocking
  - `[REDACTED]` as a named constant (not a magic string) makes the replacement marker easy to change and test against
---

## 2026-02-25 - US-046
- Installed expo-camera ^17.0.10 and expo-image-picker ^17.0.10 (--legacy-peer-deps)
- Updated app.json: added expo-camera plugin (cameraPermission → NSCameraUsageDescription on iOS, CAMERA on Android) and expo-image-picker plugin (photosPermission → NSPhotoLibraryUsageDescription on iOS)
- Created hooks/useCameraPermissions.ts: custom hook wrapping Camera.getCameraPermissionsAsync() / Camera.requestCameraPermissionsAsync() from expo-camera v17
- Hook returns: { status, isGranted, canAskAgain, isLoading, requestPermissions }
- Used PermissionStatus enum from expo-modules-core for type safety (PermissionStatus.UNDETERMINED/GRANTED/DENIED)
- getCameraPermissionsAsync() called on mount (useEffect) to check existing permissions before prompting
- requestPermissions(): calls requestCameraPermissionsAsync() and updates status + canAskAgain; returns boolean
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 66e91e8
- **Learnings for future iterations:**
  - expo-camera v17 exports `Camera` as an object (not a class) with static-like methods: `Camera.getCameraPermissionsAsync()`, `Camera.requestCameraPermissionsAsync()` — no `new Camera()` instantiation
  - PermissionStatus enum lives in expo-modules-core (not expo-camera directly): `import { PermissionStatus } from "expo-modules-core"`
  - expo-camera plugin in app.json automatically adds both NSCameraUsageDescription (iOS) and CAMERA permission (Android) — no manual infoPlist/android.permissions editing needed
  - The built-in `useCameraPermissions` from expo-camera returns a tuple `[PermissionResponse | null, requestFn, getFn]`; our custom hook in hooks/ provides a named-property API more consistent with the project's hook patterns
---

## 2026-02-25 - REFACTOR-04
- Audited components/goals/GoalProgress.tsx vs components/expenses/: uniform structure confirmed (named export, exported Props interface, functional component, NativeWind classes, no StyleSheet.create)
- Fixed props inconsistency: ExpenseCard.onPress changed from required to optional (`onPress?: () => void`) to match GoalProgress pattern; Pressable renders normally when onPress is undefined
- Verified Dashboard (tabs/index.tsx): loads expenses + goals via two independent useExpenses()/useGoals() hooks — TanStack Query fires both in parallel automatically (no useQueries needed)
- Verified tab bar (_layout.tsx): ACTIVE_COLOR #4F46E5 + INACTIVE_COLOR #6B7280, all Ionicons, zero StyleSheet.create
- Updated Codebase Patterns with 4 new patterns: onPress convention, dashboard parallel loading, tab bar brand colors, components domain module pattern
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- **Learnings for future iterations:**
  - All interactive card components (ExpenseCard, GoalProgress) should declare `onPress?: () => void` as optional — Pressable handles undefined gracefully and callers always provide it in practice
  - TanStack Query automatically parallelizes multiple useQuery hooks in the same render cycle — no need for useQueries for simple parallel fetch; use useQueries only when the number of queries is dynamic
  - components/expenses/ and components/goals/ deliberately have no index.ts barrel — imports use direct path (@/components/goals/GoalProgress) for explicit dependency tracing
---

## 2026-02-25 - US-045
- Replaced stub app/(protected)/(tabs)/profile.tsx with full implementation
- Header: indigo-600 background with circular avatar (white circle, indigo initials), display_name, email
- getInitials(): splits on whitespace, takes first char of first two words, falls back to first 2 chars of name
- Sezione 'La mia coppia': couple name row, partner row (display_name or "Nessun partner ancora"), invite code row (only shown when unclaimed code exists)
- Invite code fetched via useQuery(['invite_code', couple?.id]): queries invite_codes .is("used_by", null) .order("created_at", desc) .limit(1) .maybeSingle()
- Tapping invite code row calls Clipboard.setStringAsync() + Alert.alert("Copiato!")
- Sezione 'Privacy': two disabled/placeholder rows for "Export dati" and "Cancella account" with opacity 0.5
- Logout: Pressable with red border/text → Alert.alert confirm dialog → supabase.auth.signOut() + useAuthStore.getState().logout()
- Uses useAuthStore.getState().logout() (direct store access) since the component doesn't call useAuth() hook (avoids creating a duplicate Supabase auth subscription in a tab screen)
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 1373d5c
- **Learnings for future iterations:**
  - Call `useAuthStore.getState().logout()` directly in event handlers instead of `useAuth()` hook — tab screens should not create additional Supabase auth subscriptions (useAuth is only for root layout)
  - Invite code fetch: use `.is("used_by", null)` (not `.eq("used_by", null)`) for proper Supabase null comparison
  - Avatar initials pattern: `getInitials(name)` → split on /\s+/, take first char of first two words; fallback to `.slice(0, 2)` for single-word names
  - For disabled placeholder rows, use `style={{ opacity: 0.5 }}` + `disabled` prop rather than NativeWind opacity class for clarity
---

## 2026-02-25 - US-044
- Replaced stub app/(protected)/(tabs)/index.tsx with full Dashboard implementation
- Header: indigo-600 background, 'Ciao, [nome]!' greeting (display_name from user.user_metadata or email prefix), formatted current date in Italian (weekday + day + month + year)
- No-partner banner: amber-50/amber-200 border card with 'Invita il tuo partner' CTA navigating to /onboarding/create-couple; only shown when coupleStore.partner is null
- Balance section: white card with BalanceSummary component; receives balance from useExpenses(), partnerName from coupleStore.partner?.display_name ?? 'Partner'
- Ultime spese section: expenses.slice(0, 3) passed to ExpenseCard components; empty state with link to /expense/add
- Il vostro obiettivo section: activeGoals[0] passed to GoalProgress; empty state with link to /goals/create; onPress navigates to /goals/${id}/contribute
- All data (expenses + goals) loads in parallel via TanStack Query (two separate useQuery hooks, both fire simultaneously)
- ScrollView with contentContainerStyle={{ paddingBottom: 100 }} for full vertical scroll with FAB clearance
- Loading states: ActivityIndicator per section (expenses loading and goals loading checked independently)
- user.user_metadata?.display_name cast as `string | undefined` — user_metadata is `{ [key: string]: any }` so explicit cast avoids implicit any
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 7b8066d
- **Learnings for future iterations:**
  - Dashboard pattern: use two independent hooks (useExpenses + useGoals) — TanStack Query runs them in parallel automatically since both are useQuery calls in the same render
  - user.user_metadata?.display_name is `any` typed — always cast to `string | undefined` with `as string | undefined` for TypeScript strict compliance
  - ScrollView with heterogeneous sections (header + banner + 3 named sections) is the correct choice over FlatList for a dashboard layout
  - No-partner check: `useCoupleStore(state => state.partner) === null` is the signal; partner is set by useCouple hook after fetching couple_members
  - Empty states per section (not a single global empty state) give better UX — each section independently handles its own empty/loading state
---

## 2026-02-25 - US-043
- Updated app/(protected)/(tabs)/_layout.tsx with 4 real tabs using Ionicons
- Tab icons: Dashboard (home/home-outline), Spese (receipt/receipt-outline), Obiettivi (flag/flag-outline), Profilo (person/person-outline)
- Active tab color: #4F46E5 (indigo-600, primary brand), inactive: #6B7280 (gray-500)
- tabBarActiveTintColor + tabBarInactiveTintColor in screenOptions; focused prop drives filled vs outline icon variant
- @expo/vector-icons is bundled with Expo SDK 54 — no separate install needed
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 0e05e2c
- **Learnings for future iterations:**
  - @expo/vector-icons is included in Expo SDK 54 by default — no npm install required
  - Tab icon pattern: `tabBarIcon: ({ color, focused }) => <Ionicons name={focused ? "icon" : "icon-outline"} size={24} color={color} />` — use `color` prop (provided by react-navigation) which already reflects active/inactive state
  - tabBarActiveTintColor and tabBarInactiveTintColor in `screenOptions` applies to all tabs globally — more DRY than per-tab options
  - Ionicons icon names for Splova tabs: home/home-outline, receipt/receipt-outline, flag/flag-outline, person/person-outline
---

## 2026-02-25 - US-042
- Created app/(protected)/goals/[id]/contribute.tsx — "Aggiungi contributo" screen
- Added contributeSchema + ContributeFormData to lib/schemas/goal.ts
- Updated app/(protected)/(tabs)/goals.tsx: handleGoalPress now navigates to `/goals/${goal.id}/contribute`
- Goal info card in header: emoji + name + progress bar (static, not animated) + €current / €target + percentage
- Amount input: decimal-pad TextInput with same parseFloat/normalize pattern as add.tsx and create.tsx
- Note input: optional TextInput; `data.note?.trim() || null` handles empty string → DB null
- Submit flow: (1) insert goal_contributions; (2) compute newCurrentAmount = current + contribution; (3) update goals.current_amount (+ completed_at if completed); (4) invalidate() cache; (5) navigate back or show celebration
- Completion check: `newCurrentAmount >= goal.target_amount` → sets completed_at in same update payload
- Celebration animation: Animated.View overlay (position absolute, top/left/right/bottom 0) with Animated.parallel(opacity timing + scale spring); inner Animated.View holds content with transform:[{scale}]; auto-navigates back after 2500ms
- useNativeDriver: true for both opacity and transform (spring/timing) — native-compatible properties
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 45fafdc
- **Learnings for future iterations:**
  - For "complete on threshold" logic: `const updatePayload: { current_amount: number; completed_at?: string } = {}` — use optional property type so TypeScript accepts conditional assignment before passing to Supabase update
  - Celebration overlay: wrap the screen in `<View className="flex-1">` (not a fragment) so absolutely-positioned Animated.View fills the screen correctly
  - `Animated.parallel([timing, spring]).start()` works correctly — opacity via timing, scale via spring; both use useNativeDriver: true since opacity and transform are native-only animated properties
  - `router.push(\`/goals/${goal.id}/contribute\` as const)` pattern (same as `/expense/${id}`) is correct for typed dynamic routes with expo-router v6
  - The `[id]` directory under goals creates a nested route — Expo Router file structure: `goals/[id]/contribute.tsx` maps to URL `/goals/:id/contribute`
---

## 2026-02-25 - US-041
- Replaced stub app/(protected)/(tabs)/goals.tsx with full implementation
- Active goals: mapped GoalProgress components in px-4 container
- Completed goals: collapsible section with Pressable header showing count ("Raggiunti (N)") and ▲/▼ chevron; expanded state via useState(false)
- Pull-to-refresh: RefreshControl pattern identical to expenses.tsx; local `refreshing` state, onRefresh calls await refetch() then setRefreshing(false)
- FAB: absolute-positioned Pressable (bottom-8 right-6, w-14 h-14, bg-indigo-600 rounded-full) navigates to /goals/create
- Empty state: ScrollView wrapper with flex:1 contentContainerStyle + 🎯 emoji + motivational message "Crea il primo obiettivo condiviso e inizia a risparmiare insieme!" + inline add button
- Loading skeleton: 3x GoalSkeleton cards shown when isLoading && !refreshing
- Used ScrollView (not FlatList) — allows heterogeneous sections (active + collapsible completed) without FlatList's renderItem constraint
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 9bbdf7f
- **Learnings for future iterations:**
  - ScrollView is preferable to FlatList when the list has heterogeneous sections (e.g. active + collapsible completed group) — FlatList's sections API adds complexity without benefit for short lists
  - Collapsible section pattern: `useState(false)` for expanded state + Pressable header with ▲/▼ indicator + conditional render of children
  - Empty state needs `isEmpty` derived flag: `!isLoading && activeGoals.length === 0 && completedGoals.length === 0` — check both active AND completed to catch truly empty state
  - Router.push("/goals/create") works from tabs because the create.tsx file lives at app/(protected)/goals/create.tsx (group segment stripped by expo-router)
---

## 2026-02-25 - US-040
- Created components/goals/GoalProgress.tsx — goal progress card component
- Props: goal: GoalRow (from @/hooks/useGoals), onPress?: () => void
- Animated progress bar: Animated.Value(0) → timing to percentage (duration 800ms, useNativeDriver: false) → interpolate to "0%"–"100%" string range
- Progress color: green (#16A34A) for completed goals, indigo (#4F46E5) for active goals
- 'Raggiunto! 🎉' badge: shown when goal.completed_at !== null; green tinted pill (#DCFCE7 bg, #15803D text)
- Deadline badge: orange pill (#FFEDD5 bg, #C2410C text) with '{n}g rimasti' when !isCompleted && daysLeft >= 0 && daysLeft < 30
- Amount display: `€{current.toFixed(2)} di €{target.toFixed(2)}` left-aligned, percentage right-aligned in progress color
- overflow: "hidden" on progress bar container uses inline style (NativeWind overflow-hidden unreliable in RN)
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: faba1f2
- **Learnings for future iterations:**
  - GoalRow from @/hooks/useGoals is the correct type for goal display components (current_amount/target_amount are already Number() converted in the hook)
  - Animated.View style with `width: barWidth` (an AnimatedInterpolation<string>) requires useNativeDriver: false — consistent with US-026 pattern
  - For deadline badges, guard `daysLeft >= 0` to exclude already-passed deadlines from the "near deadline" indicator
  - `components/goals/` directory created; future goal components go here (parallel to components/expenses/)
---

## 2026-02-25 - US-039
- Created app/(protected)/goals/create.tsx — "Nuovo obiettivo" form screen
- Created lib/schemas/goal.ts — Zod schema (name required, target_amount > 0, deadline optional, emoji required)
- Also committed hooks/useGoals.ts (was untracked from previous story)
- Emoji picker: 12 emojis in a flex-wrap grid (3 columns, 48×48 touchable squares); selected emoji gets indigo border + tinted background
- Amount field: same decimal-pad + parseFloat pattern as add.tsx; large centered display
- Deadline: optional TextInput (AAAA-MM-GG placeholder); empty string normalized to null before insert
- Submit: inserts into `goals` table with couple_id from useCoupleStore; calls invalidate() then router.back()
- Pattern: `deadline?.trim() || null` is the idiomatic way to convert optional empty-string form fields to DB null
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 7c6d4ba
- **Learnings for future iterations:**
  - goals domain schema lives in lib/schemas/goal.ts (parallel to expense.ts, auth.ts, couple.ts)
  - Emoji picker grid: `w-[22%]` + flex-wrap pattern from CategoryPicker works for any fixed-size item grid; use inline style for dynamic border colors
  - `deadline?.trim() || null` handles both undefined (optional field) and empty string (user left blank) → DB null
  - `await invalidate()` before router.back() ensures TanStack Query cache is refreshed before list screen regains focus
---

## 2026-02-24 - US-037
- Created supabase/migrations/005_goals.sql
- Table goals: id (uuid PK), couple_id (FK couples CASCADE), name (text), target_amount (numeric(10,2) CHECK >0), current_amount (numeric(10,2) DEFAULT 0 CHECK >=0), deadline (date nullable), emoji (text), completed_at (timestamptz nullable), created_at
- Table goal_contributions: id (uuid PK), goal_id (FK goals CASCADE), user_id (FK auth.users RESTRICT), amount (numeric(10,2) CHECK >0), note (text nullable), created_at
- Performance indexes on goals(couple_id), goal_contributions(goal_id), goal_contributions(user_id)
- RLS enabled on both tables; reuses get_user_couple_id() helper from 002_couples.sql
- goals policies: SELECT/INSERT/UPDATE/DELETE all use `couple_id = public.get_user_couple_id()`
- goal_contributions policies: SELECT/INSERT/UPDATE/DELETE use subquery `goal_id IN (SELECT id FROM public.goals WHERE couple_id = public.get_user_couple_id())`
- Rollback section included as SQL comments
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: b97fbe4
- **Learnings for future iterations:**
  - goal_contributions has no direct couple_id — use subquery on goals table for RLS (same pattern as expense_splits → expenses)
  - user_id FK uses ON DELETE RESTRICT to preserve contribution records when a user is deleted
  - completed_at is nullable timestamptz (not date) to capture exact completion timestamp
  - current_amount has CHECK >=0 (not >0) — goals with 0 progress are valid
---

## 2026-02-24 - US-036
- Created components/expenses/BalanceSummary.tsx
- Props: balance: number, currentUserId: string, partnerName: string
- balance > 0: '[partnerName] ti deve €X' in green (#16A34A) — large 4xl bold amount
- balance < 0: 'Devi a [partnerName] €X' in orange (#EA580C) — large 4xl bold amount
- balance === 0: '🎉 Siete in pari!' with celebratory emoji, gray text
- currentUserId prop accepted but not used for display (balance sign already encodes direction)
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- **Learnings for future iterations:**
  - balance=0 check must come before balance>0 check since balance===0 is falsy — all three branches are explicit if/if/return
  - `currentUserId` prop required by spec but not needed for display logic — keep it to satisfy the AC interface contract
  - Orange for negative balance uses #EA580C (Tailwind orange-600) — red (#DC2626) feels too alarming for debt; orange is softer
---

## 2026-02-24 - US-035
- Created app/(protected)/expense/[id].tsx — expense detail screen
- Fetches single expense by ID with useQuery (queryKey: ['expense', id], .maybeSingle())
- Shows: amount (large, centered), description, category (emoji + name), date (formatted DD/MM/YYYY), payer (Tu / partner display_name)
- Shows split card: mySplit + partnerSplit from expense_splits array, each with amount and percentage
- Delete: Alert.alert confirmation dialog → supabase.delete().eq("id", id) → invalidate() → router.back()
- expense_splits cascade automatically on DB delete (FK ON DELETE CASCADE in 004_expenses.sql migration)
- invalidate() called from useExpenses() hook to refresh the expenses list on the previous screen
- Loading/error/not-found states handled explicitly
- DetailRow + SplitRow as file-local helper components (screen-specific, not reusable)
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: f42e9bf
- **Learnings for future iterations:**
  - `useLocalSearchParams<{ id: string }>()` is the correct way to get dynamic route params in expo-router typed routes
  - Alert.alert for delete confirmation: `[{ text: "Annulla", style: "cancel" }, { text: "Elimina", style: "destructive", onPress: fn }]`
  - `invalidate()` from useExpenses() invalidates the list query before navigating back — list refreshes automatically when screen regains focus via TanStack Query's `refetchOnWindowFocus`
  - expense_splits are deleted automatically by the DB CASCADE; no client-side split deletion needed
  - `.maybeSingle()` (not `.single()`) for detail fetch — returns null without error if expense not found
---

## 2026-02-24 - US-034
- Replaced stub app/(protected)/(tabs)/expenses.tsx with full implementation
- FlatList renders ExpenseRow items via ExpenseCard; keyExtractor and renderItem wrapped in useCallback
- Header: BalanceSummary inline component — shows "Il partner ti deve" (green) / "Devi al partner" (red) based on balance sign from useExpenses()
- FAB: absolute-positioned Pressable (bottom-8 right-6, w-14 h-14, bg-indigo-600 rounded-full) calls router.push("/expense/add")
- Pull-to-refresh: RefreshControl with local `refreshing` state; onRefresh calls await refetch() then setRefreshing(false)
- Empty state: EmptyState component with emoji, message "Nessuna spesa ancora. Aggiungi la prima!" and inline add button
- Loading skeleton: 5x ExpenseSkeleton rows shown when isLoading && !refreshing (avoids hiding list during pull-to-refresh)
- contentContainerStyle: `{ flex: 1 }` when empty (enables EmptyState to fill and center), `{ paddingBottom: 100 }` otherwise (clears FAB)
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: bfabf76
- **Learnings for future iterations:**
  - FlatList ListEmptyComponent needs `contentContainerStyle={{ flex: 1 }}` to allow the empty component to fill the screen; use conditional contentContainerStyle based on `expenses.length === 0`
  - Skeleton loading guard: `isLoading && !refreshing` — avoids replacing the list with skeleton during pull-to-refresh (list should stay visible while RefreshControl spinner shows)
  - FAB z-index in RN: absolutely positioned View/Pressable inside the root View naturally renders on top of FlatList — no explicit zIndex needed
  - BalanceSummary as an inline component (not a separate file) is valid for screen-specific display components that won't be reused elsewhere
---

## 2026-02-24 - US-033
- Created components/expenses/ExpenseCard.tsx
- Props: expense: ExpenseRow (from @/hooks/useExpenses — has display fields description/category/date/split_type), onPress: () => void
- Used ExpenseRow (not Expense from splitAlgorithm) because Expense is a minimal algorithm type without display fields
- Category emoji + colored bubble: CATEGORIES.find(c => c.id === expense.category) → emoji, colore; fallback "📦" / "#A8A8A8"
- Colored bubble background: `${categoryColor}22` hex alpha pattern (from US-026)
- Amount color: green (#16A34A) if expense.paid_by === userId (from useAuthStore), gray (#6B7280) otherwise
- Split type badge: SPLIT_TYPE_LABELS Record maps equal→"50/50", proportional→"Prop.", custom→"Custom"; fallback to raw string
- Touchable feedback: Pressable style callback `({ pressed }) => [{ opacity: pressed ? 0.7 : 1 }]`
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 6438078
- **Learnings for future iterations:**
  - Use ExpenseRow (from hooks/useExpenses) not Expense (from lib/splitAlgorithm) for display components — Expense is a minimal algorithm-only type
  - Pressable style callback for press feedback: `style={({ pressed }) => [{ opacity: pressed ? 0.7 : 1 }]}` — array form is valid StyleProp<ViewStyle>
  - useAuthStore() inside a display component is fine for determining "is this mine" coloring — no prop drilling needed
  - `${color}22` hex alpha pattern is reusable across components for subtle tinted backgrounds (established in US-026, used in US-029, US-033)
---

## 2026-02-24 - US-032
- Created hooks/useExpenses.ts
- Exported ExpenseSplitRow and ExpenseRow interfaces for use in UI components
- useQuery key: ['expenses', coupleId], enabled: !!coupleId
- fetchExpenses: select with nested expense_splits relation, order by date desc, limit 50
- toExpenseModel: maps DB snake_case row (paid_by → paidBy, user_id → userId) to Expense interface from lib/splitAlgorithm
- balance: calculateBalance(expenses.map(toExpenseModel), userId) — 0 when userId missing
- invalidate(): queryClient.invalidateQueries({ queryKey: ['expenses', coupleId] }) — call from mutation handlers to refresh list
- Exposed: expenses (raw ExpenseRow[]), isLoading, error, refetch, balance, invalidate
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: 5d4a4e8
- **Learnings for future iterations:**
  - Supabase nested relation syntax in select: `"expense_splits(id, user_id, amount, percentage)"` — returns array for one-to-many relations
  - Map DB snake_case to camelCase model before passing to pure algorithm functions (paid_by → paidBy, user_id → userId)
  - useQueryClient() + invalidateQueries pattern: expose invalidate() from the hook so mutation handlers can call it without needing to know the query key
  - Number() conversion on Supabase numeric columns is safe — handles both number and string-encoded numerics
---

## 2026-02-24 - US-031
- Created app/(protected)/expense/add.tsx — full "Nuova spesa" form screen
- Form fields: importo (decimal-pad TextInput → parseFloat), descrizione, CategoryPicker, data (default today YYYY-MM-DD), SplitSelector
- Local `splitShares: [number, number]` state drives SplitSelector; not stored in RHF form (schema's `shares` field is optional and unused here)
- Submit flow: (1) insert expenses row with couple_id/paid_by/amount/description/category/date/split_type; (2) compute p1/p2 amounts and percentages; (3) insert expense_splits rows; (4) rollback (delete expense) if splits insert fails; (5) router.back()
- Loading state: isSubmitting from RHF disables button and shows ActivityIndicator
- Fixed lib/schemas/expense.ts: removed `.default("equal")` from split_type — caused type mismatch between zodResolver (infers input type where field is optional) and useForm<ExpenseFormData> (uses output type where field is required); `defaultValues.split_type: "equal"` in useForm replaces the removed default
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: be28d6c
- **Learnings for future iterations:**
  - `z.enum([...]).default("value")` causes zodResolver type mismatch: resolver infers input type (field optional) but `useForm<T>` expects output type (field required) — remove `.default()` from Zod and use `defaultValues` in `useForm` instead
  - For numeric TextInput with Zod `z.number()`: use Controller where `onChange` calls `parseFloat(text)` and `value` displays `value ? String(value) : ""`; use `keyboardType="decimal-pad"` and replace commas with dots before parsing
  - SplitSelector type prop only accepts "equal"|"proportional" — compute a derived `splitTypeValue` from the form's split_type (which may also be "custom") before passing to the component
  - Supabase "transaction" pattern (no real client-side transactions): insert expense → insert splits → on splits error, delete expense; include only meaningful rollback, not full saga compensation
  - `(expenseData as { id: string }).id` is the correct typed assertion when Supabase client is untyped — avoids explicit `any` while still satisfying TypeScript strict
---

## 2026-02-24 - US-030
- Created components/expenses/SplitSelector.tsx
- Props: type ('equal'|'proportional'), onTypeChange, shares?: [number, number], onSharesChange?, amount: number
- Toggle: two-segment Pressable row (50/50 | Proporzionale) — active segment has #4F46E5 background + white text; inactive has #F9FAFB + gray text
- Proportional mode: two linked TextInput (keyboardType="number-pad") for percentages; editing one auto-updates the other to keep sum at 100
- Preview cards: Tu / Partner — shows percentage and formatted euro amount for each person
- Validation hint: red text shown when isProportional && shares[0]+shares[1] !== 100
- Dynamic toggle colors use inline `style` prop (same pattern as CategoryPicker); static text styles use NativeWind className
- `overflow: "hidden"` applied via inline style on the toggle container (NativeWind class not always reliable for overflow in RN)
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: b8c2a0c
- **Learnings for future iterations:**
  - `shares?: [number, number]` default `[50, 50]` is valid TypeScript — TS infers the default as assignable to the tuple type, so `shares[0]` and `shares[1]` are `number` (not undefined) inside the function body
  - When calling optional callback with a literal array that must match a tuple type, use `as [number, number]` cast to avoid TS widening to `number[]`
  - `overflow: "hidden"` on a container View in RN is best set via inline `style` — NativeWind's `overflow-hidden` can work but inline style is more reliable for React Native clipping behavior
  - For linked numeric inputs (must sum to 100): editing one input calculates the other as `100 - value`; use `parseInt(text, 10)` for integer percentages
  - Amount precision: give person 2 the remainder (`amount - p1Amount`) rather than computing independently to avoid cent drift on the total
---

## 2026-02-24 - US-029
- Created components/expenses/CategoryPicker.tsx
- Props: value: string, onChange: (category: string) => void
- Renders CATEGORIES (8 items) from @/constants as a flex-wrap row — 4 items per row at w-[22%] width
- Selected category: borderWidth 2, borderColor = category.colore, backgroundColor = `${colore}22` (hex+alpha tint)
- Unselected category: borderWidth 1, borderColor #E5E7EB (gray-200)
- Used inline style for dynamic borderColor (hex from data) — NativeWind cannot express dynamic hex values at runtime
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- Committed: b344702
- **Learnings for future iterations:**
  - Dynamic color values from data (e.g. category.colore hex) must use inline `style` prop, not NativeWind className — Tailwind only generates classes for statically-known values
  - w-[22%] with flex-wrap gives a natural 4-column grid for 8 items without using FlatList or numColumns
  - `${color}22` hex alpha suffix pattern (from US-026) also works here for subtle selection tint backgrounds
---

## 2026-02-24 - US-028
- Created lib/splitAlgorithm.ts with pure algorithm functions (no React Native or @/ deps)
- Types: SplitResult (array alias), Expense interface, DebtTransaction interface
- splitEqual(total, userIds): divides total equally; remainder cents go to first user
- splitProportional(total, shares): divides by relative weights; rounding correction on first entry
- calculateBalance(expenses, userId): net balance = sum(paid) - sum(owed); positive = others owe you
- simplifyDebts(balances): greedy two-pointer algorithm minimising transaction count
- Created __tests__/lib/splitAlgorithm.test.ts with 21 test cases (well above the 8-case minimum)
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS (21 new + 18 existing)
- Committed: acc82ee
- **Learnings for future iterations:**
  - SplitResult defined as an array type alias (`type SplitResult = {...}[]`) — functions return `SplitResult` directly, not `SplitResult[]`
  - Rounding pattern for financial math: `Math.round(value * 100) / 100` throughout; adjust first entry after loop to guarantee exact total
  - simplifyDebts greedy algorithm: separate positives (credits) and negatives (debts absolute values) → two-pointer sweep → O(n) transactions
  - calculateBalance: iterate expenses, add full amount if paidBy==userId, subtract split.amount if split exists — no split means no financial relation to that expense
  - Pure lib/ functions import NO React Native or @/ aliases — they compile and test under Node/Jest without any module mocking
---

## 2026-02-24 - US-027
- Created supabase/migrations/004_expenses.sql
- Table expenses: id (uuid PK), couple_id (FK couples CASCADE), paid_by (uuid FK auth.users RESTRICT), amount (numeric(10,2) CHECK >0), description (text), category (text), date (date), split_type (text CHECK IN equal/proportional/custom), created_at
- Table expense_splits: id (uuid PK), expense_id (FK expenses CASCADE), user_id (FK auth.users RESTRICT), amount (numeric(10,2)), percentage (numeric(5,2)); UNIQUE(expense_id, user_id)
- Performance indexes on expenses(couple_id), expenses(paid_by), expenses(date), expense_splits(expense_id), expense_splits(user_id)
- RLS enabled on both tables; reuses get_user_couple_id() helper from 002_couples.sql
- expenses policies: SELECT/INSERT/UPDATE/DELETE all use `couple_id = public.get_user_couple_id()`
- expense_splits policies: SELECT/INSERT/UPDATE/DELETE use subquery `expense_id IN (SELECT id FROM public.expenses WHERE couple_id = public.get_user_couple_id())`
- Rollback section included as SQL comments
- TypeScript: SUCCESS (exit 0), Jest: 18/18 PASS
- Committed: 2bcdb58
- **Learnings for future iterations:**
  - expense_splits has no direct couple_id — use subquery on expenses table for RLS membership checks (expense_id IN SELECT id FROM expenses WHERE couple_id = get_user_couple_id())
  - paid_by FK uses ON DELETE RESTRICT to preserve financial records when a user is deleted (prevents accidental data loss)
  - UNIQUE(expense_id, user_id) on expense_splits prevents duplicate split entries per user per expense
  - numeric(5,2) for percentage supports values like 33.33% (max 999.99 though semantically capped at 100)
---

## 2026-02-24 - US-026
- Replaced stub app/(protected)/onboarding/health-score-result.tsx with full implementation
- Fetches user's latest score with useQuery: `health_score_results` → `.eq("user_id", userId).order("created_at", desc).limit(1).maybeSingle()`
- Fetches partner's latest score (if couple + partner in store): same table, `.eq("couple_id", coupleId).neq("user_id", userId).maybeSingle()` — returns null on error (non-fatal)
- Count-up animation: Animated.Value(0) → animatedValue.addListener → setDisplayScore(Math.round(value)) → Animated.timing(toValue: myScore, duration: 1500, useNativeDriver: false).start()
- Colored label badge: `backgroundColor: ${scoreInfo.color}22` (hex+alpha suffix) + matching text color — driven by getScoreLabel()
- Couple score section: only shown when partnerResult?.score is non-null; uses getCoupleScore() + getScoreLabel() for its own label
- CTA: router.replace("/(protected)/(tabs)/expenses") navigates to Spese tab
- Loading state: ActivityIndicator shown while useQuery isLoading
- TypeScript: SUCCESS (exit 0), Jest: 18/18 PASS
- Committed: 0b20a84
- **Learnings for future iterations:**
  - Animated.timing with useNativeDriver: false is required when using addListener to update React state (not animating native props directly)
  - `animatedValue.addListener(cb)` returns a string listener ID — store it for removeListener cleanup in useEffect return
  - `.maybeSingle()` (not `.single()`) for queries that may return no row without throwing an error
  - Hex color + alpha suffix pattern (`${color}22`) works in React Native style backgroundColor for subtle tinted backgrounds
  - Partner score query: `.eq("couple_id", coupleId).neq("user_id", myId)` — requires couple_id to be set on the result row (done in quiz save logic)
---

## 2026-02-24 - US-025
- Updated app/(protected)/onboarding/health-score-quiz.tsx: added save + navigate logic on last answer
- Created app/(protected)/onboarding/health-score-result.tsx: minimal stub for typed routes
- On last answer: calculateHealthScore(newAnswers) → insert into health_score_results (user_id, couple_id, score, answers) → router.replace("/onboarding/health-score-result")
- isSaving state: guards against double-tap on last question; disables all options while Supabase insert is in-flight
- saveError state: shows Italian error message if insert fails, keeps user on quiz screen to retry
- couple_id sourced from useCoupleStore().couple?.id ?? null (null if user has no couple yet)
- user_id sourced from useAuthStore().user!.id (non-null assertion safe — protected route)
- TypeScript: SUCCESS (exit 0), Jest: 18/18 PASS
- Committed: 5fd4e83
- **Learnings for future iterations:**
  - When making an async Supabase call from an event handler, use a local `isSaving` state to disable UI and prevent double-submission — pattern: setIsSaving(true) → await → setIsSaving(false)
  - Non-null assertion (user!.id) is appropriate in protected routes where auth is guaranteed by the layout guard
  - couple?.id ?? null is the idiomatic way to pass optional FK that may be null when couple not yet set up
  - Route `/onboarding/health-score-result` (without group prefix) works for expo-router push/replace within protected routes — consistent with other onboarding screens (/onboarding/couple-setup, /onboarding/join-couple)
---

## 2026-02-24 - US-024
- Created app/(protected)/onboarding/health-score-quiz.tsx
- Multi-step quiz component iterating over all 6 HEALTH_SCORE_QUESTIONS from @/constants
- State: currentStep (0-indexed), answers (Record<string, number> — questionId → score)
- Animated slide using Animated.Value + useRef: slide-out then slide-in on step change
- Forward (direction=1): animates translateX to -400, resets to 400, then animates back to 0
- Backward (direction=-1): animates translateX to +400, resets to -400, then animates back to 0
- Progress bar: `style={{ width: `${progressPercent}%` }}` (percentage string as DimensionValue in RN)
- 'Indietro' button: renders transparent when step=0 (disabled prop) to maintain layout space
- Options: selectable card (Pressable) with indigo border/bg when isSelected (score match)
- isSelected uses score comparison (safe since all options per question have unique scores 0/25/50/100)
- After last question: router.replace("/(protected)/(tabs)/") — result screen in future story
- TypeScript: SUCCESS (exit 0), Jest: 18/18 PASS
- Committed: (see git)
- **Learnings for future iterations:**
  - Animated slide transition: use Animated.timing().start(callback) → setValue(inValue) → setCurrentStep → Animated.timing() in the callback (not a second start()) — avoids flash
  - Progress bar with dynamic percentage: use `style={{ width: \`${n}%\` }}` — React Native accepts percentage strings as DimensionValue
  - Use `text-transparent` with same layout as active label for invisible-but-layout-preserving back button on first step
  - After all quiz questions answered, navigate to tabs — result screen (save to DB, display score) belongs in next story
---

## 2026-02-24 - US-023
- Created lib/healthScore.ts with three pure functions: calculateHealthScore, getScoreLabel, getCoupleScore
- calculateHealthScore(answers: Record<string, number>): averages all provided scores (Object.values), returns 0 if empty, rounds with Math.round
- getScoreLabel(score): returns { label, color, description } — Ottimo (>=80, green), Buono (>=60, blue), Da migliorare (>=40, amber), Critico (<40, red)
- getCoupleScore(scoreA, scoreB): simple average rounded to nearest integer (equal weights = media pesata 50/50)
- Created __tests__/lib/healthScore.test.ts with 18 test cases covering all functions and edge cases
- Fixed babel.config.js: nativewind/babel preset requires react-native-worklets which is absent in Jest — skip it when NODE_ENV=test using `api.cache(() => process.env.NODE_ENV)` + `process.env.NODE_ENV === "test"` guard
- TypeScript: SUCCESS (exit 0), Jest: 18/18 PASS
- Committed: 9c94fca
- **Learnings for future iterations:**
  - nativewind/babel preset pulls in react-native-worklets/plugin which is NOT installed — must be excluded in Jest (NODE_ENV=test); use `api.cache(() => process.env.NODE_ENV)` (NOT api.cache(true)) when calling process.env in babel.config.js
  - lib/healthScore.ts imports NO React Native or @/ dependencies — pure math only — making it trivially testable in Jest without module mapping issues
  - Jest test files in __tests__/lib/ must import lib files with relative paths (../../lib/X) since jest-expo does NOT auto-map @/ aliases
---

## 2026-02-24 - US-022
- Created constants/healthScoreQuestions.ts
- Defined QuizCategory union type: abitudini_risparmio | gestione_debiti | comunicazione_coppia | obiettivi_finanziari
- Defined QuizOption interface: { value: string; label: string; score: number }
- Defined QuizQuestion interface: { id, text, options: QuizOption[], category: QuizCategory }
- Added HEALTH_SCORE_QUESTIONS array with 6 questions: 2 abitudini_risparmio, 1 gestione_debiti, 2 comunicazione_coppia, 1 obiettivi_finanziari
- Each question has exactly 4 options with scores 0, 25, 50, 100
- Updated constants/index.ts: added `export * from "./healthScoreQuestions"`
- TypeScript: SUCCESS (exit 0), Jest: SUCCESS (no tests)
- **Learnings for future iterations:**
  - HEALTH_SCORE_QUESTIONS is the single source of truth for quiz content — components should import from @/constants, not define questions inline
  - QuizCategory union type enables TypeScript exhaustiveness checks when mapping categories to UI labels/colors
---

## 2026-02-24 - US-021
- Created supabase/migrations/003_health_score.sql
- Table health_score_results: id (uuid PK, gen_random_uuid()), user_id (uuid FK auth.users CASCADE), couple_id (uuid nullable FK couples SET NULL), score (integer CHECK 0-100), answers (jsonb DEFAULT '{}'), created_at (timestamptz)
- Performance indexes on user_id and couple_id
- RLS enabled; SELECT policy allows own rows OR rows where couple_id matches get_user_couple_id() (partner in same couple); INSERT policy enforces user_id = auth.uid()
- Reuses existing get_user_couple_id() SECURITY DEFINER helper from 002_couples.sql for efficient couple membership lookup
- Rollback section included as SQL comments at end of file
- TypeScript: SUCCESS (exit 0), Jest: SUCCESS (no tests)
- Committed: 9aacbb5
- **Learnings for future iterations:**
  - get_user_couple_id() helper from 002_couples.sql is available for any table needing couple membership checks — avoids repeated subqueries in RLS policies
  - couple_id FK uses ON DELETE SET NULL (not CASCADE) so health score data is preserved if a couple is deleted
  - For partner visibility in RLS: `couple_id IS NOT NULL AND couple_id = get_user_couple_id()` — always guard against NULL before comparing couple_id
---

## 2026-02-24 - REFACTOR-02
- Audited error handling on all 3 auth/couple screens: sign-in, sign-up, join-couple
- Fixed sign-up.tsx: was exposing raw `error.message` from Supabase — normalized to "Registrazione non riuscita. Riprova." (consistent with sign-in's "Credenziali non valide" pattern)
- Audited form patterns: all 3 forms use identical RHF + Zod pattern (resolver, handleSubmit, formState.errors, Controller) — no changes needed
- Audited hooks: useAuth.ts and useCouple.ts serve distinct concerns (auth subscription vs TanStack Query + Zustand sync) — no duplicate utility functions found
- Audited SQL migrations: 001_profiles.sql and 002_couples.sql both have uniform structure (header comment, UP section, numbered steps, ROLLBACK section) — no changes needed
- Created lib/schemas/couple.ts: extracted joinCoupleSchema + JoinCoupleFormData from inline definition in join-couple.tsx; added createCoupleSchema for future use
- Updated join-couple.tsx: removed inline schema + z import, now imports from @/lib/schemas/couple
- TypeScript: SUCCESS (exit 0), Jest: SUCCESS (no tests)
- **Learnings for future iterations:**
  - Always normalize auth error messages to fixed Italian strings — never expose raw error.message from Supabase to users
  - All Zod schemas for forms must live in lib/schemas/ (auth.ts for auth, couple.ts for couple features) — never define inline in screen files
  - lib/schemas/couple.ts is the home for couple-domain schemas (joinCoupleSchema, createCoupleSchema)
---

## 2026-02-24 - US-020
- Created app/(public)/join/[code].tsx — deep link handler for invite codes
- `app.json` already had `scheme: "splova"` — no changes needed (Expo Router auto-maps paths from file structure)
- join/[code].tsx reads code from useLocalSearchParams<{ code: string }>(), normalizes to .toUpperCase()
- If authenticated: stores code in pendingInviteCode (Zustand), navigates to /onboarding/join-couple (code pre-filled)
- If not authenticated: stores code in pendingInviteCode, shows landing page with code displayed + "Crea account" / "Ho già un account" buttons
- Added pendingInviteCode: string | null + setPendingInviteCode to coupleStore.ts; reset() now clears it too
- Updated app/(public)/_layout.tsx: added usePathname() check — authenticated users are NOT redirected on /join/* routes (deep link handler owns its routing)
- Updated join-couple.tsx: reads pendingInviteCode from store as defaultValues.code; clears it after successful join
- Updated sign-up.tsx: after sign-up, checks pendingInviteCode; if present → router.replace to join-couple, else → couple-setup
- Updated sign-in.tsx: after sign-in, checks pendingInviteCode; if present → router.replace to join-couple, else → /(tabs)/
- TypeScript: SUCCESS (EXIT_CODE=0), Jest: SUCCESS (no tests)
- Committed: 7db371c
- **Learnings for future iterations:**
  - (public) layout redirect intercepts ALL authenticated users on public routes — use usePathname() to exempt specific routes that need custom auth handling
  - Zustand is the right tool for passing ephemeral state across auth flow boundaries (e.g. pendingInviteCode survives navigation to sign-up and back)
  - join/[code].tsx placed in app/(public)/join/[code].tsx — Expo Router strips group segments so deep link splova://join/CODE correctly maps to this file
  - For prd.json updates: the top-level key is "userStories" (not "stories")
---

## 2026-02-24 - US-019
- Replaced stub app/(protected)/onboarding/join-couple.tsx with full implementation
- Single-step form: one TextInput for the 8-char invite code
- Code input: autoCapitalize="characters", onChangeText forces .toUpperCase(), maxLength=8
- Zod schema: z.string().length(8).regex(/^[A-Z0-9]{8}$/) — covers both length and alphanum validation
- Submit flow: (1) lookup invite_codes by code; (2) check used_by IS NULL; (3) check expires_at > now(); (4) insert couple_members role=partner; (5) update invite_codes.used_by + used_at; (6) router.replace to tabs
- Clear error messages for: code not found, code already used, code expired
- update invite_codes failure is non-fatal (membership already inserted) — logs warning and continues
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: fc20aa2
- **Learnings for future iterations:**
  - For prd.json updates: top-level key is "userStories" (not "stories")
- (public) layout: use usePathname() to exempt specific public routes from the auth redirect (e.g. deep link handlers)
- pendingInviteCode in Zustand coupleStore is the pattern for passing ephemeral state across auth flow navigation boundaries
- For invite code lookup: query by .eq("code", code).single() — if error or null, code doesn't exist
  - Check used_by and expires_at client-side after fetch rather than relying solely on RLS USING clause (RLS UPDATE policy allows unclaimed+unexpired, but SELECT policy allows members — a fresh partner isn't a member yet, so the select may rely on the public/anon key access or the update RLS update_claim policy which uses USING(used_by IS NULL AND expires_at > now()))
  - autoCapitalize="characters" on TextInput + onChangeText forcing .toUpperCase() gives seamless auto-uppercase UX
  - Non-fatal DB steps: if update of invite_codes fails after membership insert, log warning and navigate — don't block user
---

## 2026-02-24 - US-018
- Replaced stub app/(protected)/onboarding/create-couple.tsx with full implementation
- Installed expo-clipboard (--legacy-peer-deps) for Clipboard.setStringAsync()
- Two-step UI: Step 1 = form with optional couple_name field; Step 2 = generated code display
- Form uses React Hook Form + Zod (z.string().max(50)) — empty string treated as null for DB
- Submit flow: (1) generateInviteCode() → 8-char UPPERCASE alphanumeric; (2) insert couples; (3) insert couple_members role=owner; (4) insert invite_codes with expires_at = now+7 days
- Code reveal screen: large code display, "Copia codice" (Clipboard.setStringAsync), "Condividi link" (Share.share with splova://join/[code] deep link), "Continua senza partner" (router.replace to tabs)
- Both "Continua senza partner" buttons (form step and code step) navigate to /(protected)/(tabs)/
- APP_SCHEME imported from @/constants (re-exports config.ts)
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: 5bad9e5
- **Learnings for future iterations:**
  - expo-clipboard must be explicitly installed (not auto-bundled in SDK 54 managed workflow package.json)
  - expo-clipboard import: `import * as Clipboard from "expo-clipboard"` then `Clipboard.setStringAsync(value)`
  - Two-step UI (form → result) pattern: use `useState<string | null>(null)` for generatedCode; if truthy render result screen, else render form
  - z.string().max(50) (not optional()) keeps Controller value typed as string — handle empty→null in submit handler with `.trim() || null`
  - Supabase multi-step insert: insert couples → get id → insert couple_members → insert invite_codes; abort on any error with setApiError
---

## 2026-02-24 - US-017
- Replaced stub app/(protected)/onboarding/couple-setup.tsx with full implementation
- useCouple() hook provides hasCouple + isLoading; useEffect redirects to /(protected)/(tabs)/ when hasCouple is true
- Shows ActivityIndicator while loading; returns null while redirect fires
- Two Pressable options: "Crea la tua coppia" → router.push("/onboarding/create-couple"), "Unisciti alla coppia del partner" → router.push("/onboarding/join-couple")
- Created stubs app/(protected)/onboarding/create-couple.tsx and join-couple.tsx (required for typedRoutes to compile)
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: 6950ed3
- **Learnings for future iterations:**
  - router.replace("/(protected)/(tabs)/") is the correct typed Href for the tabs root (group prefix required)
  - Always create stub screens for push() destinations before running tsc — typedRoutes validates all route targets
  - useEffect([isLoading, hasCouple]) + return null guard prevents flash of onboarding content before redirect fires
---

## 2026-02-23 - US-016
- Created hooks/useCouple.ts
- Exported Couple and Profile interfaces from store/coupleStore.ts for reuse
- fetchCoupleData(userId): queries couple_members → gets coupleId → parallel fetch couple + all members → resolves partner profile
- useQuery key: ['couples', userId], enabled: !!userId
- useEffect syncs query.data into Zustand coupleStore (setCouple + setPartner) on data change
- Returns: couple, partner, isLoading, hasCouple, refetch
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: cbe7c84
- **Learnings for future iterations:**
  - Export Couple/Profile interfaces from coupleStore.ts so hooks can import them (avoids duplicate type definitions)
  - TanStack Query v5: no onSuccess option — use useEffect([query.data]) to sync into Zustand
  - Multi-step Supabase queries (membership → couple → partner) done with sequential + parallel Promise.all where possible
  - Cast Supabase results explicitly (e.g. `as Couple`) since client has no Database generic type parameter
  - useQuery<CoupleData | null> generic ensures return type is correct for callers
---

## 2026-02-23 - US-015
- Created supabase/migrations/002_couples.sql
- Table couples: id (uuid PK, gen_random_uuid()), name (text nullable), created_by (uuid FK auth.users RESTRICT), created_at (timestamptz)
- Table couple_members: id (uuid PK), couple_id (FK couples CASCADE), user_id (FK auth.users CASCADE), role CHECK IN ('owner','partner'), joined_at; UNIQUE(couple_id, user_id)
- Table invite_codes: id (uuid PK), couple_id (FK couples CASCADE), code (text UNIQUE, CHECK length=8), created_by (FK auth.users RESTRICT), used_by (uuid nullable FK auth.users SET NULL), expires_at, used_at
- Performance indexes on couple_members(user_id), couple_members(couple_id), invite_codes(code), invite_codes(couple_id)
- RLS enabled on all 3 tables; helper SECURITY DEFINER function get_user_couple_id() for efficient membership checks
- RLS policies: couples (select_member, insert_auth, update_owner), couple_members (select_own_couple, insert_own, delete_own), invite_codes (select_member, insert_owner, update_claim)
- Rollback section included as SQL comments at end of file
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: ce312fb
- **Learnings for future iterations:**
  - SECURITY DEFINER helper function get_user_couple_id() avoids N+1 subqueries in RLS policies — use helper functions for repeated membership lookups
  - invite_codes UPDATE policy uses USING(used_by IS NULL AND expires_at > now()) so only unclaimed/unexpired codes can be updated by anyone, WITH CHECK(used_by = auth.uid()) ensures the claimer sets themselves
  - couple_members INSERT policy: WITH CHECK(user_id = auth.uid()) — users can only insert their own membership, covering both couple creation (owner inserts self) and invite acceptance (partner inserts self)
  - Tables with CASCADE FK on couple_id mean deleting a couple auto-removes members and invite codes
---

## 2026-02-23 - US-014
- Updated app/_layout.tsx: extracted RootLayoutNav inner component to call useAuth() for global auth initialization
- Updated app/(protected)/_layout.tsx: reads useAuthStore; shows ActivityIndicator while isLoading; redirects to /(public)/welcome via useEffect when session is null; returns null while redirect is in-flight
- Updated app/(public)/_layout.tsx: reads useAuthStore; shows ActivityIndicator while isLoading; redirects to /(tabs)/ via useEffect when session exists
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: 530f854
- **Learnings for future iterations:**
  - useAuth() must be called in root _layout.tsx (via inner RootLayoutNav) to initialize auth state before child layouts render
  - Child layouts should use useAuthStore() directly (not useAuth()) to avoid creating duplicate Supabase subscriptions
  - Pattern: useEffect([isLoading, session]) + router.replace() + return null while redirect — prevents flash of protected content
  - /(public)/welcome is a valid typed Href in Expo Router v6 with typedRoutes: true (group prefix included)
---

## 2026-02-23 - US-013
- Created hooks/useAuth.ts
- On mount: calls supabase.auth.getSession() to initialize user/session/isLoading in Zustand
- Subscribes to supabase.auth.onAuthStateChange() to keep state current; unsubscribes on unmount
- Exposes: user, session, isLoading, signOut()
- signOut() calls supabase.auth.signOut() then calls store logout() to reset user/session to null
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: c4f9b66
- **Learnings for future iterations:**
  - hooks/ directory must be created manually (doesn't exist by default)
  - setLoading(false) must be called both in getSession() .then() and in onAuthStateChange() callback to cover all paths
  - Use the store's logout() action (not setUser(null)/setSession(null) individually) for atomic reset
---

## 2026-02-23 - US-012
- Replaced stub app/(public)/sign-in.tsx with full implementation
- Form fields: email, password with React Hook Form + zodResolver(signInSchema)
- Calls supabase.auth.signInWithPassword() on submit
- Error handling: shows 'Credenziali non valide' on any auth error
- Loading: isSubmitting disables button and shows ActivityIndicator
- Navigates to /(tabs)/ via router.replace() on success (replace prevents back-nav to sign-in)
- 'Hai dimenticato la password?' placeholder link visible (Pressable, no action yet)
- Link to sign-up screen at the bottom
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: f3e8e56
- **Learnings for future iterations:**
  - Use router.replace() not router.push() after login — prevents back navigation to auth screens
  - router.replace('/(tabs)/') navigates to the protected tabs dashboard (index.tsx)
  - Error message from Supabase auth can vary — always show a fixed friendly message like 'Credenziali non valide' instead of raw error.message
---

## 2026-02-23 - US-011
- Implemented app/(public)/sign-up.tsx: full form with display_name, email, password, confirm_password
- Used React Hook Form + zodResolver(signUpSchema) for validation
- Calls supabase.auth.signUp() with display_name in user metadata options
- Loading state: isSubmitting from RHF disables button and shows ActivityIndicator
- API errors shown in red box below form fields
- Navigates to /onboarding/couple-setup on success
- Created app/(protected)/onboarding/couple-setup.tsx stub (required for typedRoutes to resolve push target)
- TypeScript: SUCCESS, Jest: SUCCESS (no tests)
- Committed: fc6d786
- **Learnings for future iterations:**
  - app/(protected)/onboarding/couple-setup.tsx → URL is /onboarding/couple-setup (group segment stripped)
  - RHF isSubmitting is true during async onSubmit — use it directly for loading/disabled state
  - supabase.auth.signUp options.data passes custom user metadata (display_name) to trigger handle_new_user()
---

## 2026-02-23 - US-010
- Updated app/(public)/welcome.tsx: logo "Splova", tagline "Finanze di coppia senza conflitti", two CTA buttons with useRouter navigation
- Created app/(public)/sign-up.tsx and sign-in.tsx as stubs (required for expo-router typed routes to accept the push() calls)
- All styling via NativeWind className — no StyleSheet.create used
- TypeScript: SUCCESS (exit 0), Jest: SUCCESS (no tests)
- Committed: dec69f1
- **Learnings for future iterations:**
  - With typedRoutes: true, router.push() to non-existent routes causes TS errors — create stub files for destination routes even if not fully implemented
  - NativeWind `gap-4` works on View for spacing between children (React Native Flexbox supports gap in RN 0.71+)
---

## 2026-02-23 - US-009
- Created supabase/migrations/001_profiles.sql (first migration)
- Table profiles: id (uuid FK auth.users CASCADE), display_name (text NOT NULL DEFAULT ''), avatar_url (text NULL), created_at/updated_at (timestamptz)
- RLS enabled with 3 policies: profiles_select_own, profiles_update_own, profiles_insert_own
- Trigger handle_new_user() SECURITY DEFINER: auto-creates profile on auth.users INSERT, uses display_name from user metadata or email prefix as fallback
- Trigger profiles_set_updated_at: auto-updates updated_at on UPDATE
- Rollback section included as SQL comments at end of file
- TypeScript: SUCCESS (no TS files changed), Jest: SUCCESS (no tests)
- **Learnings for future iterations:**
  - SQL-only migrations don't affect TypeScript checks — still run tsc to confirm no regressions
  - SECURITY DEFINER on trigger function is required so handle_new_user() can INSERT profiles bypassing RLS
  - Use COALESCE(raw_user_meta_data->>'display_name', split_part(email, '@', 1)) for sensible default display_name
  - Use DROP TRIGGER IF EXISTS before CREATE TRIGGER to make migrations idempotent
---

## 2026-02-24 - REFACTOR-03
- Audited all 4 components in components/expenses/: CategoryPicker, SplitSelector, ExpenseCard, BalanceSummary
- Fixed naming uniformity: Props interfaces renamed to ComponentNameProps pattern and exported (ExpenseCard: Props→ExpenseCardProps, BalanceSummary: Props→BalanceSummaryProps, CategoryPicker: exported CategoryPickerProps)
- Confirmed: zero StyleSheet.create usage in expenses components — all NativeWind className + inline style for dynamic hex values
- Verified useExpenses.ts follows identical pattern to useCouple.ts: [entityType, contextId] queryKey, enabled: !!contextId, typed queryFn, exposes isLoading/refetch — consistent architecture
- Verified splitAlgorithm.ts test coverage: 19 test cases across 4 functions (5+4+5+5) — well above minimum 8
- Verified add.tsx mutation pattern: pseudo-transaction (insert expense → insert splits → delete expense on splits failure) — correct Supabase client pattern
- Verified [id].tsx delete: single DELETE on expenses + ON DELETE CASCADE handles splits atomically — correct pattern
- TypeScript: SUCCESS (exit 0), Jest: 39/39 PASS
- **Learnings for future iterations:**
  - Props interface naming convention: `export interface ComponentNameProps` in every component — no bare `interface Props`; export enables consumers to import the type without coupling to the component internals
  - expenses domain has NO Zustand store (unlike auth/couple) — data lives in TanStack Query cache only; balance is derived in the hook
  - Supabase pseudo-transaction pattern is established: INSERT main row → INSERT dependent rows → on dependent error, DELETE main row (rollback); no Postgres RPC needed for MVP
  - The `invalidate()` function in useExpenses is the standard pattern for post-mutation cache refresh — any screen that mutates expenses should call it
---

## Codebase Patterns
- Bash tool runs in MINGW64 on Windows but output is NOT returned to caller — redirect all output to files with `> file.log 2>&1` then read with Read tool
- Use `&& echo "SUCCESS" >> out.log || echo "FAILED" >> out.log` pattern to capture exit status reliably
- The SPLOVA root IS the app root (not a nested splova/ subdirectory)
- tsconfig.json MUST have `"exclude": ["node_modules", "repository github", "ralph", "tasks", "documentazione"]` to avoid TS errors from reference repos
- package.json jest config MUST have `testPathIgnorePatterns` with `/repository github/` etc. to avoid Jest scanning reference repos
- Expo SDK 54 uses expo-router ~6.0.0 (NOT v4 as in CLAUDE.md), React 19.1.0, RN 0.81.4
- Version pinning from reference repo `expo-supabase-starter`: expo-constants ~18.0.7, expo-linking ~8.0.7, expo-splash-screen ~31.0.8, expo-status-bar ~3.0.7, react-native-safe-area-context ~5.6.0, react-native-screens ~4.16.0
- npm install requires `--legacy-peer-deps` for this project
- Git working directory: `/c/Users/Tempocasa P.ta Volta/SPLOVA` (bash uses forward slashes despite Windows)
- Branch: `ralph/splova-mvp-v2` (already created)
- NativeWind v4 setup: global.css + metro.config.js withNativeWind + babel preset nativewind/babel + tailwind.config.js with nativewind/preset
- NativeWind v4 does NOT need react-native-reanimated as peer dep (only tailwindcss)
- Gluestack UI v3: uses @gluestack-ui/core@^3 (not @gluestack-ui/themed v2). Components are source code, not package imports
- @/* path alias already configured in tsconfig.json from US-001 setup
- Zod v4 (latest): z.number() does NOT support invalid_type_error option. Use chain: z.number().positive("msg")
- constants/ always has an index.ts re-exporting all modules; import via `@/constants` not individual files
- tailwind.config.js content covers only UI dirs: ./app/**/* and ./components/**/* (not lib/, store/, hooks/)
- app.json has typedRoutes: true (expo-router typed routes enabled — use typed Href where needed)
- Clean log files after use — don't commit .log files to git
- All Zod schemas live in lib/schemas/: auth.ts for auth screens, couple.ts for couple features — never define inline in screen files
- Auth error messages must always be normalized fixed Italian strings (never raw error.message from Supabase)
- Form pattern (RHF + Zod): `useForm<T>({ resolver: zodResolver(schema) })` → `control, handleSubmit, formState: { errors, isSubmitting }` → Controller per field → apiError state for API-level errors
- babel.config.js: use `api.cache(() => process.env.NODE_ENV)` (NOT api.cache(true)) when conditionally excluding presets by env; nativewind/babel must be skipped in Jest (NODE_ENV=test) as it requires react-native-worklets which is not installed
- Jest test files in __tests__/: use relative imports (../../lib/X) for lib/ files — jest-expo does NOT auto-map @/ tsconfig path aliases
- Props interface naming: always `export interface ComponentNameProps` — never bare `interface Props`; exporting enables type reuse by consumers
- expenses domain has NO Zustand store — data lives only in TanStack Query cache; balance is derived synchronously in useExpenses hook from the cached list
- Supabase pseudo-transaction: INSERT main → INSERT dependents → DELETE main on dependent error; ON DELETE CASCADE handles child row cleanup for deletes
- useExpenses.invalidate() is the standard post-mutation cache refresh; any screen mutating expenses should call it after success
- UI component onPress convention: always declare `onPress?: () => void` (optional) in Props interface — both ExpenseCard and GoalProgress follow this; callers always provide it but the component renders without it too
- Dashboard parallel data loading: two independent `useQuery` hooks (useExpenses + useGoals) in the same component fire in parallel automatically — no need for useQueries or Promise.all
- Tab bar brand colors: ACTIVE_COLOR = "#4F46E5" (indigo-600), INACTIVE_COLOR = "#6B7280" (gray-500) — defined as constants at module level in (tabs)/_layout.tsx
- components/expenses/ and components/goals/ follow identical module pattern: single .tsx file per component, named export, exported Props interface, NativeWind classes, inline style only for dynamic hex colors — no index.ts barrel in domain component directories
- Ephemeral cross-screen state (e.g. pendingInviteCode, pendingReceipt) lives in coupleStore as Zustand fields — cleared by the consumer after use; this avoids URL param size limits and keeps state strongly-typed
---

# Ralph Progress Log
Started: Mon Feb 23 11:50:32     2026
---

## 2026-02-23 - REFACTOR-01
- Audited all config files: tsconfig.json, babel.config.js, metro.config.js, tailwind.config.js — no duplications or inconsistencies found
- Audited constants/: types and structure correct; added constants/index.ts re-exporting all modules
- Audited all _layout.tsx files: uniform structure, clean imports confirmed
- Audited all components/ui/*, lib/*, store/* files: no unused imports found
- TypeScript: SUCCESS (exit 0)
- Jest: "No tests found, exiting with code 0" (SUCCESS)
- **Learnings for future iterations:**
  - Always add constants/index.ts so imports use `@/constants` not `@/constants/config` etc.
  - Use `&& echo "SUCCESS" >> log || echo "FAILED" >> log` for reliable exit code capture in MINGW64
  - app.json typedRoutes: true is already enabled — future route navigation should use typed Href<Route>
---

## 2026-02-23 - US-008
- Created constants/config.ts: APP_NAME, SUPABASE_URL/ANON_KEY (process.env ?? ''), APP_VERSION, APP_SCHEME
- Created constants/categories.ts: 8 categories (Casa, Cibo, Trasporti, Salute, Svago, Shopping, Viaggi, Altro) with id/nome/emoji/colore
- Created constants/pricing.ts: FREE_PLAN, PREMIUM_PLAN with Plan interface and LIMITS object
- TypeScript: exit 0
- Committed: 2546de3
---

## 2026-02-23 - US-007
- Installed react-hook-form, zod@4.3.6, @hookform/resolvers (--legacy-peer-deps)
- Created lib/schemas/auth.ts: signUpSchema and signInSchema with Zod v4
- Created lib/schemas/expense.ts: expenseSchema placeholder
- Fixed Zod v4 incompatibility: z.number() does NOT accept invalid_type_error in v4
- TypeScript: exit 0
- Committed: 3c3ff28
- **Learnings for future iterations:**
  - Zod v4 installed (not v3): z.number() no longer accepts {invalid_type_error} option
  - Zod v4: chain validation methods with string message: .positive("msg"), not {message: "msg"}
---

## 2026-02-23 - US-006
- Installed zustand and @tanstack/react-query (--legacy-peer-deps)
- Created store/authStore.ts with Zustand: user, session, isLoading, setUser, setSession, setLoading, logout
- Created store/coupleStore.ts with Zustand: couple, partner, setCouple, setPartner, reset
- Updated app/_layout.tsx to wrap with QueryClientProvider above GluestackUIProvider
- TypeScript: exit 0
- Committed: c79b648
- **Learnings for future iterations:**
  - QueryClientProvider must be defined at module level (not inside component) to avoid re-creation on re-render
  - coupleStore needs both setCouple and setPartner (PRD only mentions setCouple)
  - authStore isLoading: true initially, set false after first auth check
---

## 2026-02-23 - US-005
- Installed @supabase/supabase-js and expo-secure-store (--legacy-peer-deps)
- Created lib/supabase.ts with ExpoSecureStoreAdapter using getItemAsync/setItemAsync/deleteItemAsync
- .env.local already existed with real Supabase credentials (preserved)
- .env.local already gitignored; created .env.example with empty keys
- TypeScript: exit 0
- Committed: 56799f7
- **Learnings for future iterations:**
  - .env.local may already exist with real credentials — check before overwriting
  - ExpoSecureStoreAdapter: getItem→getItemAsync, setItem→setItemAsync, removeItem→deleteItemAsync
---

## 2026-02-23 - US-004
- Installed @gluestack-ui/core@^3 and @gluestack-ui/utils@^3 (v3 packages, not v2 @gluestack-ui/themed)
- Created minimal component wrappers in components/ui/: GluestackUIProvider, Button/ButtonText, Text, Input/InputField, Box, VStack, HStack
- All components use React Native primitives, no complex external dependencies
- Created components/ui/index.ts re-exporting all components
- Updated app/_layout.tsx to wrap with GluestackUIProvider mode="light"
- TypeScript: exit 0
- Committed: 8a2e2c4
- **Learnings for future iterations:**
  - Gluestack UI v3 uses @gluestack-ui/core@^3, NOT @gluestack-ui/themed (v2 - deprecated)
  - Gluestack v3 components are "copy-pasted" source code, not just npm imports
  - Simple React Native wrappers suffice for MVP setup story; extend later with full Gluestack v3 source
  - @/components/ui path alias works via tsconfig.json paths: `"@/*": ["./*"]` (already set up in US-001)
---

## 2026-02-23 - US-003
- Installed nativewind@^4 and tailwindcss@^3 (--legacy-peer-deps)
- Created: global.css, tailwind.config.js (with nativewind/preset), metro.config.js (withNativeWind), nativewind-env.d.ts
- Updated babel.config.js to add nativewind/babel preset
- Updated app/_layout.tsx to import global.css
- Updated welcome.tsx to use className props to verify NativeWind works
- TypeScript: exit 0
- Committed: 91549c6
- **Learnings for future iterations:**
  - NativeWind v4 (not v2): requires global.css + metro withNativeWind, not just babel plugin
  - NativeWind v4 only peer dep is tailwindcss (no react-native-reanimated needed for base install)
  - tailwind.config.js must use `presets: [require('nativewind/preset')]` in NativeWind v4
  - babel.config.js: `presets: ["babel-preset-expo", "nativewind/babel"]` (second entry is the NativeWind preset)
  - global.css must be imported in root _layout.tsx: `import "../global.css";`
---

## 2026-02-23 - US-002
- All layout files were already created in US-001; only added placeholder auth guard comment to protected layout
- Updated app/(protected)/_layout.tsx with TODO comment for US-014 auth guard
- TypeScript check: exit 0, Jest: no tests / exit 0
- Committed: 4579031
- **Learnings for future iterations:**
  - US-002 criteria were already satisfied by US-001 implementation (layouts existed)
  - Protected layout placeholder auth guard can be a TODO comment; real implementation deferred to US-014
  - git commit command must use `tee` not `>` redirection to capture output (tee works with bash stdout)
---

## 2026-02-23 - US-001
- Initialized Expo SDK 54 project structure directly in SPLOVA root
- Created: package.json, tsconfig.json, app.json, babel.config.js, expo-env.d.ts, .gitignore
- Created: app/_layout.tsx (Root Stack), app/(public)/_layout.tsx, app/(public)/welcome.tsx
- Created: app/(protected)/_layout.tsx, app/(protected)/(tabs)/_layout.tsx with 4 tab placeholders
- Created: app/(protected)/(tabs)/index.tsx, expenses.tsx, goals.tsx, profile.tsx
- Copied assets from expo-supabase-starter reference repo
- **Learnings for future iterations:**
  - CLAUDE.md says "Expo Router v4" but SDK 54 actually uses expo-router v6 — use reference repo versions
  - React version 18.3.2 does not exist; React 19.1.0 is correct for Expo SDK 54
  - tsconfig.json `include: ["**/*.ts", "**/*.tsx"]` picks up reference repo files — must add `exclude` list
  - Jest preset `jest-expo` scans entire codebase including reference repos — add `testPathIgnorePatterns`
  - The `repository github/` directory contains full repo clones with their own tests that would break our test suite
  - bash tool output is not visible in tool return — write to log files and read them back
---
